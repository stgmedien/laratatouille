Single choice among a handful of short options — date, service, seating.

```jsx
<RadioGroup label="Bereich" value={area} onChange={setArea}
  options={["Innen","Terrasse","Chef's Table"]} />
```

Use for 2–5 options; beyond that switch to `Select`. Selected state is a pine border with sage-100 fill.
