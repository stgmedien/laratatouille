Consent and opt-in control. Controlled: pass `checked` and `onChange`.

```jsx
<Checkbox id="news" checked={news} onChange={setNews}
  label="Newsletter" description="Alle vier Wochen, neue Karte und Weinabende." />
```
